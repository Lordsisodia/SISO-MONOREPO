# Tasks Index

| Folder | Scope | Notes |
| --- | --- | --- |
| `core-roadmap/` | 14-task platform build sequence | migrated from `/tasks`; retains original README + task files. |
| `client-app-sprint/` | Aug 2025 “Analysis One” fixes | moved out of `/03-insights` so implementation lives with other sprints. |

Add future sprints as `YYYY-MM-name/` entries with a short README summarizing the goal, owner, and links back to the insight pack that spawned it.  
